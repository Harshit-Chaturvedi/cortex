# core
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0

# LLM providers (system tries them in order, falls back automatically)
langchain-groq>=0.2.0
langchain-google-genai>=2.0.0
langchain-huggingface>=0.1.0
langchain-openai>=0.3.0

# vector store
langchain-chroma>=0.2.0
chromadb>=0.5.0

# embeddings — fastembed uses ONNX (no PyTorch), much lighter than sentence-transformers
# fits comfortably in 512MB RAM on free hosting tiers
fastembed>=0.3.0

# document loaders
pypdf>=4.0

# api
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# config
python-dotenv>=1.0
